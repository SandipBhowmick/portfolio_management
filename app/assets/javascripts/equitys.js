// Place all the behaviors and hooks related to the matching controller here.
// All this logic will automatically be available in application.js.

 /*$(document).on('click','#scrip-name',function() {
	$.ajax({
			type: "get",
			url: "/equitys",
			dataType: "json",
			success:function(result){
				$.each(result)
				}
			})

 	});*/



/*$(function(){
	$('#scrip-name').autocomplete({
		
		minLength: 2,
		source: @equitys,

		

	focus: function(event, ui) {
                $('#scrip-name').val(ui.item.equity.code);
                return false;
            },
    });
});*/