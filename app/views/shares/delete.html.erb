<%= link_to("< Back to Main", {:action => 'index', :share_id =>@share.id}, :class => 'back-link') %>
<div class ="new share">
	<h2> Delete a transaction detail</h2>
	<%= form_for(:share, :url => {:action => 'destroy', :id => @share.id}) do |f| %>
		<p> Are you sure you want to permanently delete this transaction detail</p>
		<p class="reference-name"><%= @share.scrip_name %></p>
		<div class="form-buttons">
			<%= submit_tag("Delete Transaction detail") %>
		</div>
	<% end %>
</div>

